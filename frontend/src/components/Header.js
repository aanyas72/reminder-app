import "../styles/Header.css";

const Header = () => {
  return (
    <div className="header">
      <button className="logo">company logo</button>
      <button className="user-logo">user logo</button>
    </div>
  );
};

export default Header;
